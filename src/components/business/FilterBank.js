import React, { Component } from 'react';

export class FilterBank extends Component {
  state = {
    stdId: 0,
    stdUserName: ''
  };

  render() {
    return (
      <div className="FilterBank">
      </div>
    );
  }
}

export default FilterBank;
